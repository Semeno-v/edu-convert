# EduConvert — зависимости (Python 3.14+)
#
# Единственный источник истины — pyproject.toml. Здесь только ссылка на него,
# иначе два списка неизбежно расходятся: в прошлой версии этого файла рантайм
# уже был перемешан с pytest/tox.
#
#   pip install -r requirements.txt        # рантайм + инструменты разработки
#   pip install -e .                       # только рантайм

-e .[dev]
